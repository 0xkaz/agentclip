{
  "name": "@agentclip/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:prod": "node build.mjs --prod",
    "package": "node build.mjs --prod && cd dist && zip -qr ../agentclip-extension.zip . && echo 'created apps/extension/agentclip-extension.zip'",
    "typecheck": "echo \"no typecheck\" && exit 0",
    "lint": "echo \"no lint\" && exit 0",
    "test": "echo \"no tests\" && exit 0"
  }
}
